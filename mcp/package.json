{
  "name": "kobil-profile-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server exposing KOBIL profile/identity actions (read user, update attributes, trigger password/email change) to AI agents.",
  "type": "module",
  "bin": {
    "kobil-profile-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
